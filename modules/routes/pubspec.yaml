name: routes
description: A new Flutter package project.
version: 0.0.1
homepage:

publish_to: "none"

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^5.2.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  auth:
    path: ../auth
  docs:
    path: ../docs
  courses:
    path: ../courses
  profile:
    path: ../profile
  dashboard:
    path: ../dashboard
  shared:
    path: ../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  import_sorter: ^4.6.0

import_sorter:
  comments: false

flutter:
