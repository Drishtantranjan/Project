import 'package:flutter/material.dart';

class EditBasic extends StatelessWidget {
  const EditBasic({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Edit Qualification'),
    );
  }
}
