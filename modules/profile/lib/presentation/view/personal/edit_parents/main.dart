import 'package:flutter/material.dart';

class EditParents extends StatelessWidget {
  const EditParents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Edit Qualification'),
    );
  }
}
