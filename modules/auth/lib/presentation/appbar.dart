import 'package:flutter/material.dart';

import 'package:shared/button.dart';

class AuthAppbar extends StatelessWidget {
  const AuthAppbar({super.key});

  @override
  Widget build(BuildContext context) {
    return const PopButton();
  }
}
